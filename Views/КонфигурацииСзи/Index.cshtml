@model IEnumerable<Compromise.Models.КонфигурацииСзи>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ОписаниеОповещения)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.УровеньОпасности)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Id)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ОписаниеОповещения)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.УровеньОпасности)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Id.IdИндикатора)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdОповещения">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdОповещения">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdОповещения">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
