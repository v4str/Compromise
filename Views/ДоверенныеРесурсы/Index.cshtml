@model IEnumerable<Compromise.Models.ДоверенныеРесурсы>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IpАдрес)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.НазваниеОрганизации)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Id)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IpАдрес)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.НазваниеОрганизации)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Id.IdИндикатора)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdРесурса">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdРесурса">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdРесурса">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
