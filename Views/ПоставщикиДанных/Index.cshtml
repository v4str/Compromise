@model IEnumerable<Compromise.Models.ПоставщикиДанных>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Поставщик)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.РейтингДоверия)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Поставщик)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.РейтингДоверия)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdПоставщика">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdПоставщика">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdПоставщика">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
